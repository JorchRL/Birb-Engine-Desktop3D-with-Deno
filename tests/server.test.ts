// Tests for /lib/server

import { describe, it } from "./deps.ts";

// in order to test all of this effectively, I think I need to refactor a lot of the code into exported modules. Instead of just having imperative scripts laying around.

// So I'm still a bit far off from something usable hahaha

describe("Server behaviour", () => {
  it('should serve files from root url "/" given a folder', () => {
  });
});
